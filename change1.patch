diff --git a/.github/workflows/trade-runner.yml b/.github/workflows/trade-runner.yml
index b63f7cad3e71a8b71060d02dd35e5ec8a95deb57..e9bba21b39157f9d18df3c84da7f88e76b0ccb19 100644
--- a/.github/workflows/trade-runner.yml
+++ b/.github/workflows/trade-runner.yml
@@ -99,50 +99,55 @@ jobs:
       APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
       API_BASE_URL: ${{ secrets.KIS_REST_URL }}
 
     steps:
       - name: Checkout code (always run nullim code)
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
           ref: "nullim"
           path: repo
 
       - name: Assert Postgres-only + SSOT (startup)
         shell: bash
         run: |
           set -euo pipefail
           : "${PBCORE_DB_URL:?Missing PBCORE_DB_URL}"
           if [[ ! "${PBCORE_DB_URL}" =~ ^postgres(ql)?:// ]]; then
             echo "::error::PBCORE_DB_URL must be postgres-only. got=${PBCORE_DB_URL}"
             exit 1
           fi
           if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
             echo "::error::repo/bot_state exists. SSOT violated."
             ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
             exit 1
           fi
+          if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
+            echo "::error::repo contains tracked bot_state. SSOT violated."
+            git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
+            exit 1
+          fi
 
       - name: Checkout bot-state branch
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
           ref: "bot-state"
           path: botstate
           token: ${{ secrets.GITHUB_TOKEN }}
 
       - name: Compute BOT_STATE_DIR (runtime)
         shell: bash
         run: |
           set -euo pipefail
           BOTSTATE_WORKTREE_DIR="${GITHUB_WORKSPACE}/botstate"
           echo "BOTSTATE_WORKTREE_DIR=${BOTSTATE_WORKTREE_DIR}" >> "$GITHUB_ENV"
           echo "BOT_STATE_DIR=${BOTSTATE_WORKTREE_DIR}/bot_state" >> "$GITHUB_ENV"
           mkdir -p "${BOTSTATE_WORKTREE_DIR}/bot_state"
           echo "[BOTSTATE_WORKTREE_DIR]=${BOTSTATE_WORKTREE_DIR}"
           echo "[BOT_STATE_DIR]=${BOTSTATE_WORKTREE_DIR}/bot_state"
 
       - name: Set SSOT absolute paths (universe_build)
         shell: bash
         run: |
           set -euo pipefail
           {
@@ -292,52 +297,52 @@ jobs:
             git -C "$BOTSTATE_WORKTREE_DIR" config user.name "github-actions"
             git -C "$BOTSTATE_WORKTREE_DIR" commit -m "bot_state: universe build $(date -u +%F)"
             git -C "$BOTSTATE_WORKTREE_DIR" push origin HEAD:bot-state
           fi
 
       - name: Verify persisted universe (botstate)
         if: env.SHOULD_BUILD_UNIVERSE == '1'
         run: |
           set -euo pipefail
           python - << 'PY'
           import json
           import os
 
           env = (os.getenv("KIS_ENV") or "practice").lower()
           path = f"botstate/bot_state/universe_lkg/{env}/best_k_meta/latest.json"
           with open(path, "r", encoding="utf-8") as handle:
               data = json.load(handle)
           print("[VERIFY] lkg members=", len(data.get("members", [])))
           PY
 
       - name: Assert SSOT (no repo/bot_state exists)
         if: always()
         shell: bash
         run: |
           set -euo pipefail
-          # NOTE: grep-based check was a false-positive (matched workflow/patch text).
-          # SSOT intent: ensure repo does not contain an actual bot_state directory.
+          # NOTE: grep-based check is banned (false positives).
+          # SSOT intent: ensure repo does not contain actual bot_state directory or tracked files.
           if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
             echo "ERROR: repo/bot_state exists. SSOT violated."
             ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
             exit 1
           fi
           # additionally fail if tracked by git (defensive)
           if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
             echo "ERROR: repo contains tracked bot_state. SSOT violated."
             git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
             exit 1
           fi
           echo "OK: SSOT holds (no repo/bot_state)."
 
   trade_tick:
     runs-on: ubuntu-latest
     if: github.event_name != 'schedule' || github.event.schedule != '45 23 * * 0-4'
     timeout-minutes: 25
 
     env:
       TZ: Asia/Seoul
       PYTHONUNBUFFERED: "1"
       PYTHONPATH: ${{ github.workspace }}/repo
 
       FORCE_STRATEGY_MODE: ""
       FORCE_PB1_PHASE: ""
@@ -363,74 +368,75 @@ jobs:
       KIS_ENV: ${{ secrets.KIS_ENV }}
       CANO: ${{ secrets.CANO }}
       ACNT_PRDT_CD: ${{ secrets.ACNT_PRDT_CD }}
       KIS_ACCESS_TOKEN: ${{ secrets.KIS_ACCESS_TOKEN }}
       KIS_ACCOUNT: ${{ secrets.KIS_ACCOUNT }}
 
       APP_KEY: ${{ secrets.KIS_APP_KEY }}
       APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
       API_BASE_URL: ${{ secrets.KIS_REST_URL }}
 
       PB1_RUN_LOOP_MINUTES: "15"
       PB1_MAX_MINUTES: "15"
       PB1_MAX_SECONDS: "900"
 
       BOTSTATE_LOCK_TTL_SEC: "1500"
       BOTSTATE_LOCK_BUFFER_SEC: "240"
       BOTSTATE_LOCK_RETRY_TOTAL_SEC: "70"
       BOTSTATE_LOCK_RETRY_SLEEP_SEC: "3"
 
       KIS_HTTP_MAX_RETRIES: "5"
       KIS_HTTP_BACKOFF_BASE_SEC: "1.2"
       KIS_HTTP_BACKOFF_CAP_SEC: "8"
 
       ACTIVE_STRATEGIES: "1"
       DAILY_CAPITAL: ${{ secrets.KIS_ENV == 'practice' && '10000000' || '250000000' }}
-      LIVE_TRADING_ENABLED: "1"
 
     steps:
       - name: Checkout code (always run nullim code)
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
           ref: "nullim"
           path: repo
 
-      - name: Assert Postgres-only + SSOT (startup)
+      - name: Assert SSOT (startup)
         shell: bash
         run: |
           set -euo pipefail
-          : "${PBCORE_DB_URL:?Missing PBCORE_DB_URL}"
-          if [[ ! "${PBCORE_DB_URL}" =~ ^postgres(ql)?:// ]]; then
-            echo "::error::PBCORE_DB_URL must be postgres-only. got=${PBCORE_DB_URL}"
-            exit 1
-          fi
+          # NOTE: grep-based check is banned (false positives).
+          # SSOT intent: ensure repo does not contain actual bot_state directory or tracked files.
           if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
             echo "::error::repo/bot_state exists. SSOT violated."
             ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
             exit 1
           fi
+          if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
+            echo "::error::repo contains tracked bot_state. SSOT violated."
+            git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
+            exit 1
+          fi
 
       - name: Checkout bot-state branch
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
           ref: "bot-state"
           path: botstate
 
       - name: Set botstate paths (SSOT)
         id: botstate_paths
         shell: bash
         run: |
           set -euo pipefail
           REPO_DIR="${GITHUB_WORKSPACE}/repo"
           BOTSTATE_REPO_DIR="${GITHUB_WORKSPACE}/botstate"
           BOTSTATE_WORKTREE_DIR="${GITHUB_WORKSPACE}/_botstate_worktree/${GITHUB_RUN_ID}_${GITHUB_JOB}"
           BOT_STATE_DIR="${BOTSTATE_WORKTREE_DIR}/bot_state"
 
           {
             echo "REPO_DIR=${REPO_DIR}"
             echo "BOTSTATE_REPO_DIR=${BOTSTATE_REPO_DIR}"
             echo "BOTSTATE_WORKTREE_DIR=${BOTSTATE_WORKTREE_DIR}"
             echo "BOT_STATE_DIR=${BOT_STATE_DIR}"
           } >> "$GITHUB_ENV"
 
@@ -542,117 +548,170 @@ jobs:
           is_weekday = is_trading_weekday(now)
           window = calc_market_window_kst(now)
           in_market = is_weekday and window in {"preopen", "morning", "day", "close"}
 
           mode = "LIVE" if in_market else "DIAG"
           if force_mode in {"LIVE", "DIAG", "INTENT_ONLY", "INTENT"}:
               mode = "LIVE" if force_mode == "LIVE" else "DIAG"
 
           if force_phase in {"entry", "exit", "verify"}:
               pb1_phase = force_phase
           else:
               pb1_phase = "entry" if window in {"preopen", "morning", "day"} else "exit"
 
           print(
               f"[AUTO] now={now.isoformat()} trading_day={is_weekday} window={window} "
               f"in_market={in_market} -> STRATEGY_MODE={mode} PB1_PHASE_DEFAULT={pb1_phase}"
           )
 
           with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as f:
               f.write(f"EFFECTIVE_STRATEGY_MODE={mode}\n")
               f.write(f"TRADING_DAY={'true' if is_weekday else 'false'}\n")
               f.write(f"MARKET_WINDOW={window}\n")
               f.write(f"PB1_PHASE_DEFAULT={pb1_phase}\n")
           PY
 
+      - name: Set LIVE_TRADING_ENABLED default
+        shell: bash
+        run: |
+          set -euo pipefail
+          if [ -z "${LIVE_TRADING_ENABLED:-}" ]; then
+            echo "LIVE_TRADING_ENABLED=1" >> "$GITHUB_ENV"
+          fi
+
       - name: Configure DIAG full rehearsal
         if: env.DIAG_FULL_REHEARSAL == '1'
         shell: bash
         run: |
           set -euo pipefail
           {
             echo "PB1_DIAG_REHEARSAL=1"
             echo "DIAG_ALLOW_NONTRADING_RUN=1"
             echo "FORCE_STRATEGY_MODE=DIAG"
             echo "FORCE_MARKET_WINDOW=morning"
             echo "FORCE_PB1_PHASE=entry"
             echo "LIVE_TRADING_ENABLED=0"
             echo "DIAG_KIS_CALLS_ENABLED=${DIAG_REHEARSAL_KIS_CALLS}"
           } >> "$GITHUB_ENV"
           if [ "${DIAG_REHEARSAL_KIS_CALLS}" = "0" ]; then
             {
               echo "PB1_REQUIRE_BALANCE_FOR_ENTRY=0"
               echo "PB1_PREOPEN_REQUIRE_BALANCE=0"
             } >> "$GITHUB_ENV"
           fi
 
+      - name: Gate: disable live trading in DIAG
+        shell: bash
+        run: |
+          set -euo pipefail
+          mode="${EFFECTIVE_STRATEGY_MODE:-}"
+          if [ "$mode" = "DIAG" ] || [ "${FORCE_STRATEGY_MODE:-}" = "DIAG" ]; then
+            echo "[GATE] DIAG detected -> force LIVE_TRADING_ENABLED=0"
+            echo "LIVE_TRADING_ENABLED=0" >> "$GITHUB_ENV"
+          fi
+
       - name: Compile check (gated)
         if: env.COMPILE_CHECK_ENABLED == '1'
         run: |
           set -euo pipefail
           python -m compileall trader
 
       - name: Configure git identity for botstate commits
         run: |
           git config --global user.email "actions@github.com"
           git config --global user.name "github-actions[bot]"
 
       - name: Force nontrading smoke once (KST) when requested
         if: ${{ github.event_name == 'workflow_dispatch' && inputs.VERIFY_NONTRADING_SMOKE == '1' }}
         shell: bash
         run: |
           set -euo pipefail
 
           # KST 날짜 계산
           KST_DATE="$(python - <<'PY'
           from datetime import datetime, timezone, timedelta
           kst = timezone(timedelta(hours=9))
           print(datetime.now(kst).date().isoformat())
           PY
           )"
           FLAG="$BOT_STATE_DIR/runtime/diagnostics/nontrading_smoke_once_${KST_DATE}.flag"
 
           echo "[VERIFY_NONTRADING_SMOKE] KST_DATE=$KST_DATE"
           echo "[VERIFY_NONTRADING_SMOKE] Removing flag if exists: $FLAG"
 
           rm -f "$FLAG" || true
           # diagnostics 폴더가 없을 수도 있으니 생성
           mkdir -p "$BOT_STATE_DIR/runtime/diagnostics"
           ls -la "$BOT_STATE_DIR/runtime/diagnostics" || true
 
+      - name: Guard: postgres-only + SSOT + DIAG safety
+        shell: bash
+        run: |
+          set -euo pipefail
+
+          # 1) Postgres-only 강제 (스킴만 출력해서 원인 파악 가능)
+          : "${PBCORE_DB_URL:?Missing PBCORE_DB_URL}"
+          scheme="$(echo "$PBCORE_DB_URL" | sed -E 's#^([^:]+):.*#\1#')"
+          echo "[GUARD] PBCORE_DB_URL scheme=$scheme"
+
+          if [[ ! "${PBCORE_DB_URL}" =~ ^postgres(ql)?:// ]]; then
+            echo "::error::PBCORE_DB_URL must start with postgres:// or postgresql://"
+            exit 1
+          fi
+
+          # 2) SSOT 강제: repo/bot_state 존재 금지 (실제 디렉토리만 체크)
+          if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
+            echo "::error::repo/bot_state exists. SSOT violated."
+            ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
+            exit 1
+          fi
+
+          # 3) repo에 bot_state가 tracked로 들어왔는지 체크
+          if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
+            echo "::error::repo contains tracked bot_state. SSOT violated."
+            git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
+            exit 1
+          fi
+
+          echo "[GUARD] OK: postgres-only + SSOT"
+
       - name: Run PB1 (auto window) - should exit within 4 minutes
         shell: bash
         env:
           BOTSTATE_WORKTREE_DIR: ${{ steps.botstate_paths.outputs.botstate_worktree_dir }}
           BOT_STATE_DIR: ${{ steps.botstate_paths.outputs.bot_state_dir }}
           NONTRADING_SMOKE_FORCE: ${{ github.event_name == 'workflow_dispatch' && inputs.VERIFY_NONTRADING_SMOKE || '0' }}
           DATABASE_URL: ${{ secrets.DATABASE_URL }}
           TRADER_CACHE_ROOT: ${{ env.TRADER_CACHE_ROOT }}
           UNIVERSE_LKG_DIR: ${{ env.UNIVERSE_LKG_DIR }}
           PBCORE_DB_URL: ${{ secrets.PBCORE_DB_URL }}
           STATE_PATH: ${{ env.STATE_PATH }}
           LOT_STATE_PATH: ${{ env.LOT_STATE_PATH }}
+          LIVE_TRADING_ENABLED: ${{ env.LIVE_TRADING_ENABLED }}
+          DIAG_FULL_REHEARSAL: ${{ env.DIAG_FULL_REHEARSAL }}
+          DIAG_REHEARSAL_KIS_CALLS: ${{ env.DIAG_REHEARSAL_KIS_CALLS }}
+          PB1_BLOCK_ORDERS: ${{ env.EFFECTIVE_STRATEGY_MODE == 'DIAG' && '1' || '0' }}
         run: |
           set -euo pipefail
           cd "$REPO_DIR"
           if [ "${DIAG_FULL_REHEARSAL}" = "1" ]; then
             echo "[RUN] DIAG rehearsal enabled (window=morning phase=entry)"
             python -m trader.run --window morning --phase entry
           else
             echo "[RUN] STRATEGY_MODE=$EFFECTIVE_STRATEGY_MODE MARKET_WINDOW=$MARKET_WINDOW PB1_PHASE_DEFAULT=$PB1_PHASE_DEFAULT"
             python -m trader.run --window auto --phase auto
           fi
 
       - name: Verify DIAG rehearsal outputs
         if: env.DIAG_FULL_REHEARSAL == '1'
         shell: bash
         run: |
           set -euo pipefail
           python - << 'PY'
           import json
           import os
           from datetime import date
 
           from trader.db.engine import make_engine
           from trader.db.repos import OrdersRepo, UniverseRepo
 
           env = (os.getenv("KIS_ENV") or "practice").lower()
@@ -694,96 +753,100 @@ jobs:
         run: |
           set -euo pipefail
           reset_flag="$BOT_STATE_DIR/runtime/reset_flags/$(TZ=Asia/Seoul date +%F).json"
           if [ -f "$reset_flag" ]; then
             echo "[RESET][HARD][FLAG] $reset_flag"
             cat "$reset_flag"
           else
             echo "::error::reset flag missing: $reset_flag"
             exit 1
           fi
 
       - name: Inspect bot_state git status (post tick)
         if: ${{ steps.botstate_git.outputs.is_git_repo == 'true' }}
         run: |
           set -euo pipefail
           cd "$BOTSTATE_WORKTREE_DIR"
           echo "[BOTSTATE][GIT] workdir=$(pwd)"
           git status --porcelain | head -n 200 || true
           git diff --cached --name-only | head -n 200 || true
 
       - name: Debug bot_state contents (after run)
         if: always()
         run: |
           set -euo pipefail
           echo "=== bot_state tree ==="
-          find "$BOT_STATE_DIR" -maxdepth 3 -type f -print || true
+          if [ -n "${BOT_STATE_DIR:-}" ] && [ -d "${BOT_STATE_DIR}" ]; then
+            find "$BOT_STATE_DIR" -maxdepth 3 -type f -print || true
+          else
+            echo "BOT_STATE_DIR is not set or dir missing -> skip tree dump"
+          fi
           echo "=== git status (workspace) ==="
           git status --porcelain || true
 
       - name: Persist bot_state to bot-state branch
         if: ${{ steps.botstate_git.outputs.is_git_repo == 'true' }}
         shell: bash
         run: |
           set -euo pipefail
           echo "[PERSIST] BOTSTATE_WORKTREE_DIR=$BOTSTATE_WORKTREE_DIR"
 
           # 변경 없으면 종료
           if [ -z "$(git -C "$BOTSTATE_WORKTREE_DIR" status --porcelain)" ]; then
             echo "[PERSIST] no changes -> skip"
             exit 0
           fi
 
           git -C "$BOTSTATE_WORKTREE_DIR" add -A
           if git -C "$BOTSTATE_WORKTREE_DIR" status --porcelain | grep -E '\.sqlite3|\.db|bot_state/db' >/dev/null 2>&1; then
             echo "::error::Forbidden sqlite/db paths in botstate changes."
             git -C "$BOTSTATE_WORKTREE_DIR" status --porcelain | head -n 200
             exit 1
           fi
           git -C "$BOTSTATE_WORKTREE_DIR" commit -m "bot_state: ${GITHUB_RUN_ID}/${GITHUB_JOB} $(date -u +%F_%T)" || true
           git -C "$BOTSTATE_WORKTREE_DIR" push origin HEAD:bot-state
 
       - name: Scan for SQLite artifacts
         if: always()
         shell: bash
         run: |
           set -euo pipefail
           hits="$(find "$GITHUB_WORKSPACE" -type f \( -name "*.sqlite3*" -o -name "*.db" \) 2>/dev/null | head -n 50 || true)"
           if [ -n "$hits" ]; then
             echo "::error::SQLite artifacts detected (forbidden):"
             echo "$hits"
             exit 1
           fi
           echo "OK: no sqlite artifacts."
 
       - name: Assert SSOT (no repo/bot_state exists)
         if: always()
         shell: bash
         run: |
           set -euo pipefail
-          # NOTE: grep-based check was a false-positive (matched workflow/patch text).
-          # SSOT intent: ensure repo does not contain an actual bot_state directory.
+          # NOTE: grep-based check is banned (false positives).
+          # SSOT intent: ensure repo does not contain actual bot_state directory or tracked files.
           if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
             echo "ERROR: repo/bot_state exists. SSOT violated."
             ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
             exit 1
           fi
           if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
             echo "ERROR: repo contains tracked bot_state. SSOT violated."
             git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
             exit 1
           fi
           echo "OK: SSOT holds (no repo/bot_state)."
 
   close_cancel:
     runs-on: ubuntu-latest
     if: github.event_name == 'schedule' && github.event.schedule == '29 6 * * 1-5'
     timeout-minutes: 15
 
     env:
       TZ: Asia/Seoul
       PYTHONUNBUFFERED: "1"
       PYTHONPATH: ${{ github.workspace }}/repo
       BOT_STATE_HARD_RESET: ${{ github.event.inputs.BOT_STATE_HARD_RESET || '0' }}
 
       FORCE_STRATEGY_MODE: ""
       FORCE_PB1_PHASE: ""
@@ -809,64 +872,66 @@ jobs:
 
       APP_KEY: ${{ secrets.KIS_APP_KEY }}
       APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
       API_BASE_URL: ${{ secrets.KIS_REST_URL }}
 
       BOTSTATE_LOCK_TTL_SEC: "1500"
       BOTSTATE_LOCK_BUFFER_SEC: "240"
       BOTSTATE_LOCK_RETRY_TOTAL_SEC: "55"
       BOTSTATE_LOCK_RETRY_SLEEP_SEC: "5"
 
       KIS_HTTP_MAX_RETRIES: "5"
       KIS_HTTP_BACKOFF_BASE_SEC: "1.2"
       KIS_HTTP_BACKOFF_CAP_SEC: "8"
 
       ACTIVE_STRATEGIES: "1"
       DAILY_CAPITAL: ${{ secrets.KIS_ENV == 'practice' && '10000000' || '250000000' }}
 
     steps:
       - name: Checkout code (always run nullim code)
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
           ref: "nullim"
           path: repo
 
-      - name: Assert Postgres-only + SSOT (startup)
+      - name: Assert SSOT (startup)
         shell: bash
         run: |
           set -euo pipefail
-          : "${PBCORE_DB_URL:?Missing PBCORE_DB_URL}"
-          if [[ ! "${PBCORE_DB_URL}" =~ ^postgres(ql)?:// ]]; then
-            echo "::error::PBCORE_DB_URL must be postgres-only. got=${PBCORE_DB_URL}"
-            exit 1
-          fi
+          # NOTE: grep-based check is banned (false positives).
+          # SSOT intent: ensure repo does not contain actual bot_state directory or tracked files.
           if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
             echo "::error::repo/bot_state exists. SSOT violated."
             ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
             exit 1
           fi
+          if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
+            echo "::error::repo contains tracked bot_state. SSOT violated."
+            git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
+            exit 1
+          fi
 
       - name: Checkout bot-state branch
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
           ref: "bot-state"
           path: botstate
 
       - name: Compute BOT_STATE_DIR (runtime)
         shell: bash
         run: |
           set -euo pipefail
           BOTSTATE_WORKTREE_DIR="${GITHUB_WORKSPACE}/botstate"
           echo "BOTSTATE_WORKTREE_DIR=${BOTSTATE_WORKTREE_DIR}" >> "$GITHUB_ENV"
           echo "BOT_STATE_DIR=${BOTSTATE_WORKTREE_DIR}/bot_state" >> "$GITHUB_ENV"
           mkdir -p "${BOTSTATE_WORKTREE_DIR}/bot_state"
           echo "[BOTSTATE_WORKTREE_DIR]=${BOTSTATE_WORKTREE_DIR}"
           echo "[BOT_STATE_DIR]=${BOTSTATE_WORKTREE_DIR}/bot_state"
 
       - name: Set SSOT absolute paths (close_cancel)
         shell: bash
         run: |
           set -euo pipefail
           {
             echo "TRADER_CACHE_ROOT=${BOT_STATE_DIR}/runtime"
@@ -886,50 +951,81 @@ jobs:
 
       - name: Setup Python
         uses: actions/setup-python@v5
         with:
           python-version: "3.11"
 
       - name: Cache pip
         uses: actions/cache@v4
         with:
           path: ~/.cache/pip
           key: ${{ runner.os }}-pip-${{ hashFiles('repo/requirements.txt') }}
           restore-keys: |
             ${{ runner.os }}-pip-
 
       - name: Install deps
         run: |
           set -euo pipefail
           python -m pip install --upgrade pip
           if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
 
       - name: Configure git identity for botstate commits
         run: |
           git config --global user.email "actions@github.com"
           git config --global user.name "github-actions[bot]"
 
+      - name: Guard: postgres-only + SSOT + DIAG safety
+        shell: bash
+        run: |
+          set -euo pipefail
+
+          # 1) Postgres-only 강제 (스킴만 출력해서 원인 파악 가능)
+          : "${PBCORE_DB_URL:?Missing PBCORE_DB_URL}"
+          scheme="$(echo "$PBCORE_DB_URL" | sed -E 's#^([^:]+):.*#\1#')"
+          echo "[GUARD] PBCORE_DB_URL scheme=$scheme"
+
+          if [[ ! "${PBCORE_DB_URL}" =~ ^postgres(ql)?:// ]]; then
+            echo "::error::PBCORE_DB_URL must start with postgres:// or postgresql://"
+            exit 1
+          fi
+
+          # 2) SSOT 강제: repo/bot_state 존재 금지 (실제 디렉토리만 체크)
+          if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
+            echo "::error::repo/bot_state exists. SSOT violated."
+            ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
+            exit 1
+          fi
+
+          # 3) repo에 bot_state가 tracked로 들어왔는지 체크
+          if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
+            echo "::error::repo contains tracked bot_state. SSOT violated."
+            git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
+            exit 1
+          fi
+
+          echo "[GUARD] OK: postgres-only + SSOT"
+
       - name: Run PB1 close-cancel (auto window)
         run: |
           set -euo pipefail
           python -m trader.pb1_runner --window auto --phase auto
         env:
           BOT_STATE_DIR: ${{ env.BOT_STATE_DIR }}
           TRADER_CACHE_ROOT: ${{ env.TRADER_CACHE_ROOT }}
           UNIVERSE_LKG_DIR: ${{ env.UNIVERSE_LKG_DIR }}
           PBCORE_DB_URL: ${{ secrets.PBCORE_DB_URL }}
           STATE_PATH: ${{ env.STATE_PATH }}
           LOT_STATE_PATH: ${{ env.LOT_STATE_PATH }}
 
       - name: Detect botstate git repo
         id: botstate_git
         shell: bash
         run: |
           set -euo pipefail
           if [ -n "${BOTSTATE_WORKTREE_DIR:-}" ] && [ -d "${BOTSTATE_WORKTREE_DIR}/.git" ]; then
             echo "is_git_repo=true" >> "$GITHUB_OUTPUT"
           else
             echo "is_git_repo=false" >> "$GITHUB_OUTPUT"
           fi
           echo "[BOTSTATE_GUARD] BOTSTATE_WORKTREE_DIR=${BOTSTATE_WORKTREE_DIR:-<empty>} is_git_repo=$(cat $GITHUB_OUTPUT | tail -n 1)"
 
       - name: Persist bot_state (guarded)
@@ -978,38 +1074,38 @@ jobs:
           # 3) 최종 clean 보장 (여기서 실패하면 진짜 문제)
           if [[ -n "$(git status --porcelain)" ]]; then
             echo "::error::botstate worktree dirty after persist"
             git status --porcelain | head -n 200
             exit 1
           fi
 
       - name: Scan for SQLite artifacts
         if: always()
         shell: bash
         run: |
           set -euo pipefail
           hits="$(find "$GITHUB_WORKSPACE" -type f \( -name "*.sqlite3*" -o -name "*.db" \) 2>/dev/null | head -n 50 || true)"
           if [ -n "$hits" ]; then
             echo "::error::SQLite artifacts detected (forbidden):"
             echo "$hits"
             exit 1
           fi
           echo "OK: no sqlite artifacts."
 
       - name: Assert SSOT (no repo/bot_state exists)
         if: always()
         shell: bash
         run: |
           set -euo pipefail
-          # NOTE: grep-based check was a false-positive (matched workflow/patch text).
-          # SSOT intent: ensure repo does not contain an actual bot_state directory.
+          # NOTE: grep-based check is banned (false positives).
+          # SSOT intent: ensure repo does not contain actual bot_state directory or tracked files.
           if [ -e "$GITHUB_WORKSPACE/repo/bot_state" ]; then
             echo "ERROR: repo/bot_state exists. SSOT violated."
             ls -la "$GITHUB_WORKSPACE/repo/bot_state" || true
             exit 1
           fi
           if git -C "$GITHUB_WORKSPACE/repo" ls-files --error-unmatch "bot_state" >/dev/null 2>&1; then
             echo "ERROR: repo contains tracked bot_state. SSOT violated."
             git -C "$GITHUB_WORKSPACE/repo" ls-files "bot_state" || true
             exit 1
           fi
           echo "OK: SSOT holds (no repo/bot_state)."
diff --git a/trader/kis_wrapper.py b/trader/kis_wrapper.py
index df352ba995a2157a4190f91f07b5dd26f0847353..da9fbe86704af6c08e8f954744c0c120cd697b71 100644
--- a/trader/kis_wrapper.py
+++ b/trader/kis_wrapper.py
@@ -58,50 +58,54 @@ class KisAuthError(Exception):
     """401/403 인증 오류."""
 
 
 class KisPermanentError(Exception):
     """기타 4xx 등 영구 오류."""
 
 
 class KisBalanceUnavailable(KisTemporaryError):
     """잔고 조회 실패."""
 
 
 class NetTemporaryError(KisTemporaryError):
     """네트워크/SSL 등 일시적 오류를 의미 (제외 금지, 루프 스킵)."""
 
 
 class DataEmptyError(Exception):
     """정상응답이나 캔들이 0개 (실제 데이터 없음)."""
     pass
 
 
 class DataShortError(Exception):
     """정상응답이나 캔들이 need_n 미만."""
     pass
 
 
+class OrderBlockedError(RuntimeError):
+    """주문 하드 가드에 의해 차단된 경우."""
+
+
 def _build_session():
     s = requests.Session()
     retry = Retry(
         total=0, connect=0, read=0, status=0,
         backoff_factor=0,
         status_forcelist=[],
         allowed_methods=["GET", "POST"],
         raise_on_status=False,
     )
     adapter = HTTPAdapter(max_retries=retry, pool_connections=50, pool_maxsize=50)
     s.mount("https://", adapter)
     s.mount("http://", adapter)
     s.headers.update({"User-Agent": "RKMax/1.0", "Connection": "keep-alive"})
     return s
 
 
 SESSION = _build_session()
 
 
 def _get_json(url, params=None, timeout=(3.0, 7.0)):
     try:
         r = SESSION.get(url, params=params, timeout=timeout)
         r.raise_for_status()
         return r.json()
     except requests.exceptions.SSLError as e:
@@ -276,58 +280,61 @@ def _is_raw_balance_snapshot(snapshot: Any) -> bool:
     if isinstance(output2, list) and output2:
         first = output2[0]
         return isinstance(first, dict) and bool(first)
     if isinstance(output2, dict) and output2:
         return True
     return False
 
 
 def _order_block_reason(now: datetime | None = None) -> Optional[str]:
     now = now or now_kst()
     state_date = _ORDER_BLOCK_STATE.get("date")
     state_reason = _ORDER_BLOCK_STATE.get("reason")
     if state_date and state_date != now.date():
         _ORDER_BLOCK_STATE.update({"date": None, "reason": None})
         state_date, state_reason = None, None
     if state_date == now.date() and state_reason:
         return str(state_reason)
     if not is_trading_day(now):
         _ORDER_BLOCK_STATE.update({"date": now.date(), "reason": "NON_TRADING_DAY"})
         return "NON_TRADING_DAY"
     if not is_trading_window(now):
         return "OUTSIDE_TRADING_WINDOW"
     return None
 
 
-def _assert_orders_allowed() -> None:
-    if os.getenv("PB1_DIAG_REHEARSAL") == "1":
-        raise RuntimeError("orders forbidden in rehearsal")
-    mode = (os.getenv("STRATEGY_MODE") or "").strip().upper()
-    live_flag = (os.getenv("LIVE_TRADING_ENABLED") or "").strip()
-    if mode != "LIVE" or live_flag != "1":
-        raise RuntimeError(
-            f"orders forbidden (STRATEGY_MODE={mode or 'unset'} LIVE_TRADING_ENABLED={live_flag or 'unset'})"
+def _assert_orders_allowed(action: str) -> None:
+    block = (os.getenv("PB1_BLOCK_ORDERS") or "").strip().lower() in {"1", "true", "yes", "y"}
+    diag_rehearsal = (os.getenv("PB1_DIAG_REHEARSAL") or "").strip().lower() in {"1", "true", "yes", "y"}
+    mode = (os.getenv("EFFECTIVE_STRATEGY_MODE") or os.getenv("STRATEGY_MODE") or "").strip().upper()
+    live = (os.getenv("LIVE_TRADING_ENABLED") or "").strip().lower() in {"1", "true", "yes", "y"}
+
+    if block or diag_rehearsal or mode == "DIAG" or not live:
+        raise OrderBlockedError(
+            "Orders are blocked. "
+            f"action={action} PB1_BLOCK_ORDERS={os.getenv('PB1_BLOCK_ORDERS')} "
+            f"mode={mode} LIVE_TRADING_ENABLED={os.getenv('LIVE_TRADING_ENABLED')}"
         )
 
 
 def _mark_order_blocked(reason: str, now: datetime | None = None) -> None:
     now = now or now_kst()
     _ORDER_BLOCK_STATE.update({"date": now.date(), "reason": reason})
 
 
 def _is_order_disallowed(resp: Any) -> Optional[str]:
     if not isinstance(resp, dict):
         return None
     msg1 = str(resp.get("msg1") or "")
     msg_cd = str(resp.get("msg_cd") or "")
     msg = f"{msg1} {msg_cd}".strip()
     primary_phrases = ("영업일이 아닙니다", "주문 가능 시간이 아닙니다", "주문가능시간이 아닙니다")
     if any(p in msg1 for p in primary_phrases):
         return msg or "ORDER_NOT_ALLOWED"
 
     low = msg.lower()
     keywords = ("휴장", "가능시간", "closed")
     if any(k in low for k in keywords):
         return msg or "ORDER_NOT_ALLOWED"
     status = resp.get("_status")
     if isinstance(status, int) and status in (401, 403):
         return f"HTTP_{status}"
@@ -2146,51 +2153,51 @@ class KisAPI:
                     last_err = exc
                     logger.warning("[RECONCILE][FAIL] tr_id=%s err=%s", tr_id, exc)
                     return _empty_daily_ccld("PERM_FAIL")
                 except requests.exceptions.RequestException as exc:
                     last_err = exc
                     logger.warning("[RECONCILE][FAIL] tr_id=%s err=%s", tr_id, exc)
                     return _empty_daily_ccld("REQ_FAIL")
                 except Exception as exc:
                     last_err = exc
                     logger.warning("[RECONCILE][FAIL] tr_id=%s err=%s", tr_id, exc)
                     return _empty_daily_ccld("TEMP_FAIL")
             if attempt < len(backoff_seq):
                 backoff = backoff_seq[attempt - 1]
                 jitter = random.uniform(0.0, min(0.2, backoff * 0.2))
                 time.sleep(backoff + jitter)
         if last_err:
             logger.warning("[RECONCILE][DEGRADED] daily_ccld_failed err=%s", last_err)
         self._reset_session()
         return _empty_daily_ccld("TEMP_FAIL")
 
     # -------------------------------
     # 주문 공통, 시장가/지정가, 매수/매도
     # -------------------------------
     def _order_cash(self, body: dict, *, is_sell: bool) -> Optional[dict]:
         url = f"{API_BASE_URL}/uapi/domestic-stock/v1/trading/order-cash"
-        _assert_orders_allowed()
+        _assert_orders_allowed("order_cash")
 
         # TR 후보 순차 시도
         tr_list = _pick_tr(self.env, "ORDER_SELL" if is_sell else "ORDER_BUY")
 
         now = now_kst()
         block_reason = _order_block_reason(now)
         if block_reason:
             logger.warning("[ORDER_BLOCK] %s code=%s qty=%s", block_reason, body.get("PDNO"), body.get("ORD_QTY"))
             return {"rt_cd": "1", "msg_cd": "ORDER_BLOCK", "msg1": block_reason, "output": {}}
 
         # Fallback: 시장가 → IOC시장가 → 최유리
         ord_dvsn_chain = ["01", "13", "03"]
         last_err = None
 
         for tr_id in tr_list:
             for ord_dvsn in ord_dvsn_chain:
                 body["ORD_DVSN"] = ord_dvsn
                 body["ORD_UNPR"] = "0"
                 if is_sell and not body.get("SLL_TYPE"):
                     body["SLL_TYPE"] = "01"
                 body.setdefault("EXCG_ID_DVSN_CD", "KRX")
 
                 # HashKey
                 try:
                     hk = self._create_hashkey(body)
@@ -2340,51 +2347,51 @@ class KisAPI:
                     f"last={last} age={now_ts-last:.1f}s"
                 )
                 return {"status": "SKIPPED", "skip_reason": "DUP_BLOCK"}
 
         body = {
             "CANO": self.CANO,
             "ACNT_PRDT_CD": self.ACNT_PRDT_CD,
             "PDNO": safe_strip(pdno),
             "SLL_TYPE": "01",  # 일반매도
             "ORD_QTY": str(int(qty)),
             "ORD_DVSN": "01",
             "ORD_UNPR": "0",
             "EXCG_ID_DVSN_CD": "KRX",
         }
         resp = self._order_cash(body, is_sell=True)
         if resp and isinstance(resp, dict) and resp.get("rt_cd") == "0":
             with self._recent_sells_lock:
                 self._recent_sells[pdno] = time.time()
                 cutoff = time.time() - (self._recent_sells_cooldown * 5)
                 keys_to_del = [k for k, v in self._recent_sells.items() if v < cutoff]
                 for k in keys_to_del:
                     del self._recent_sells[k]
         return resp
 
     def buy_stock_limit(self, pdno: str, qty: int, price: int) -> Optional[dict]:
-        _assert_orders_allowed()
+        _assert_orders_allowed("buy_stock_limit")
         now = now_kst()
         block_reason = _order_block_reason(now)
         if block_reason:
             logger.warning("[ORDER_BLOCK] %s code=%s qty=%s", block_reason, pdno, qty)
             return {"rt_cd": "1", "msg_cd": "ORDER_BLOCK", "msg1": block_reason, "output": {}}
 
         body = {
             "CANO": self.CANO,
             "ACNT_PRDT_CD": self.ACNT_PRDT_CD,
             "PDNO": safe_strip(pdno),
             "ORD_QTY": str(int(qty)),
             "ORD_DVSN": "00",  # 지정가
             "ORD_UNPR": str(int(price)),
             "EXCG_ID_DVSN_CD": "KRX",
         }
         hk = self._create_hashkey(body)
         tr_list = _pick_tr(self.env, "ORDER_BUY")
         if not tr_list:
             raise Exception("ORDER_BUY TR 미구성")
         tr_id = tr_list[0]
         headers = self._headers(tr_id, hk)
         url = f"{API_BASE_URL}/uapi/domestic-stock/v1/trading/order-cash"
         # [CHG] 안전요청 사용
         resp = self._safe_request(
             "POST", url, headers=headers, data=_json_dumps(body).encode("utf-8"), timeout=(3.0, 7.0)
@@ -2395,51 +2402,51 @@ class KisAPI:
             try:
                 out = data.get("output") or {}
                 odno = out.get("ODNO") or out.get("ord_no") or ""
                 pdno = safe_strip(body.get("PDNO", ""))
                 qty_int = int(float(body.get("ORD_QTY", "0")))
                 price_for_fill = float(body.get("ORD_UNPR", 0))
                 append_fill(
                     side="BUY",
                     code=pdno,
                     name="",
                     qty=qty_int,
                     price=price_for_fill,
                     odno=odno,
                     note=f"limit,tr={tr_id}",
                 )
             except Exception as e:
                 logger.warning(f"[APPEND_FILL_LIMIT_BUY_FAIL] ex={e}")
             return data
         logger.error(f"[BUY_LIMIT_FAIL] {data}")
         blocked = _is_order_disallowed(data)
         if blocked:
             _mark_order_blocked(blocked, now)
         return None
 
     def sell_stock_limit(self, pdno: str, qty: int, price: int) -> Optional[dict]:
-        _assert_orders_allowed()
+        _assert_orders_allowed("sell_stock_limit")
         now = now_kst()
         block_reason = _order_block_reason(now)
         if block_reason:
             logger.warning("[ORDER_BLOCK] %s code=%s qty=%s", block_reason, pdno, qty)
             return {"rt_cd": "1", "msg_cd": "ORDER_BLOCK", "msg1": block_reason, "output": {}}
 
         # --- 강화된 사전점검: 보유수량 우선 ---
         pos = self.get_positions() or []
         hldg = 0
         ord_psbl = 0
         for r in pos:
             if safe_strip(r.get("pdno")) == safe_strip(pdno):
                 hldg = int(float(r.get("hldg_qty", "0")))
                 ord_psbl = int(float(r.get("ord_psbl_qty", "0")))
                 break
 
         base_qty = hldg if hldg > 0 else ord_psbl
         if base_qty <= 0:
             logger.error(
                 f"[SELL_LIMIT_PRECHECK] 보유 없음/수량 0 pdno={pdno} hldg={hldg} ord_psbl={ord_psbl}"
             )
             return None
 
         if qty > base_qty:
             logger.warning(
