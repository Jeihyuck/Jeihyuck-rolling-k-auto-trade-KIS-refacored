diff --git a/.github/workflows/trade-runner.yml b/.github/workflows/trade-runner.yml
index 5d036b7ad31104124d47e6f209151e4207953c43..c5964be2f1b415fdbf2c049285ece2e5f566555e 100644
--- a/.github/workflows/trade-runner.yml
+++ b/.github/workflows/trade-runner.yml
@@ -1,58 +1,224 @@
 name: "Trade Runner (AUTO: LIVE in market, DIAG otherwise) [5m tick]"
 
 permissions:
   contents: write
 
 concurrency:
   group: trade-bot-state
   cancel-in-progress: false
 
 on:
   # ✅ push로도 즉시 돌고
   push:
     branches: [ "nullim", "main" ]
 
   # ✅ 수동 실행도 되고
   workflow_dispatch:
+    inputs:
+      EMERGENCY_UNIVERSE_BUILD:
+        description: "Set to 1 to allow one-time emergency universe build when DB is missing."
+        required: false
+        default: "0"
 
   # ✅ 5분 tick (스케줄 안정성을 위해 main에도 이 파일 존재 권장)
   schedule:
     - cron: "50-59/5 23 * * 0-4"
     - cron: "45 23 * * 0-4"
     - cron: "*/5 0-5 * * 1-5"
     - cron: "0-30/5 6 * * 1-5"
     - cron: "29 6 * * 1-5"
 
 defaults:
   run:
     shell: bash
     working-directory: repo
 
 jobs:
-  run:
+  universe_build:
     runs-on: ubuntu-latest
+    if: (github.event_name == 'schedule' && github.event.schedule == '45 23 * * 0-4') || github.event_name == 'workflow_dispatch'
+    timeout-minutes: 15
+
+    env:
+      TZ: Asia/Seoul
+      PYTHONUNBUFFERED: "1"
+      PYTHONPATH: ${{ github.workspace }}/repo
+      STATE_PATH: "bot_state/runtime/state.json"
+      LOT_STATE_PATH: "bot_state/runtime/lot_state.json"
+      PBCORE_DB_PATH: "bot_state/db/pbcore.sqlite3"
+      TRADER_CACHE_ROOT: "bot_state/runtime"
+      UNIVERSE_LKG_DIR: "bot_state/universe_lkg"
+      BOTSTATE_WORKTREE_DIR: "${{ github.workspace }}/_botstate"
+      EMERGENCY_UNIVERSE_BUILD: ${{ inputs.EMERGENCY_UNIVERSE_BUILD }}
+
+      KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
+      KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
+      KIS_REST_URL: ${{ secrets.KIS_REST_URL }}
+      KIS_ENV: ${{ secrets.KIS_ENV }}
+      CANO: ${{ secrets.CANO }}
+      ACNT_PRDT_CD: ${{ secrets.ACNT_PRDT_CD }}
+      KIS_ACCESS_TOKEN: ${{ secrets.KIS_ACCESS_TOKEN }}
+      KIS_ACCOUNT: ${{ secrets.KIS_ACCOUNT }}
+
+      APP_KEY: ${{ secrets.KIS_APP_KEY }}
+      APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
+      API_BASE_URL: ${{ secrets.KIS_REST_URL }}
+
+    steps:
+      - name: Checkout code (always run nullim code)
+        uses: actions/checkout@v4
+        with:
+          fetch-depth: 0
+          ref: "nullim"
+          path: repo
+
+      - name: Checkout bot-state branch
+        uses: actions/checkout@v4
+        with:
+          fetch-depth: 0
+          ref: "bot-state"
+          path: botstate
+
+      - name: Restore bot_state into workspace
+        run: |
+          set -euo pipefail
+          mkdir -p bot_state
+          if [ -d ../botstate/bot_state ]; then
+            rsync -a --delete --exclude='.gitkeep' --exclude='.gitignore' ../botstate/bot_state/ bot_state/
+          fi
+          echo "[RESTORE] repo/bot_state restored"
+
+      - name: Setup Python
+        uses: actions/setup-python@v5
+        with:
+          python-version: "3.11"
+
+      - name: Cache pip
+        uses: actions/cache@v4
+        with:
+          path: ~/.cache/pip
+          key: ${{ runner.os }}-pip-${{ hashFiles('repo/requirements.txt') }}
+          restore-keys: |
+            ${{ runner.os }}-pip-
+
+      - name: Install deps
+        working-directory: repo
+        run: |
+          set -euo pipefail
+          python -m pip install --upgrade pip
+          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
+
+      - name: Resolve universe build gate
+        working-directory: repo
+        run: |
+          set -euo pipefail
+          as_of="$(TZ=Asia/Seoul date +%F)"
+          now_kst="$(TZ=Asia/Seoul date -Iseconds)"
+          echo "AS_OF_DATE=$as_of" >> "$GITHUB_ENV"
+          if [ "${GITHUB_EVENT_NAME}" = "schedule" ]; then
+            echo "SHOULD_BUILD_UNIVERSE=1" >> "$GITHUB_ENV"
+            echo "[UNIVERSE][SCHEDULED_BUILD] now_kst=$now_kst as_of=$as_of"
+            exit 0
+          fi
+          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ] && [ "${EMERGENCY_UNIVERSE_BUILD:-0}" = "1" ]; then
+            python - << 'PY'
+            from datetime import date
+            import os
+
+            from trader.db.engine import make_engine
+            from trader.db.repos import UniverseRepo
+
+            env = (os.getenv("KIS_ENV") or "practice").lower()
+            strategy = os.getenv("PB1_UNIVERSE_STRATEGY") or "best_k_meta"
+            as_of = date.today().isoformat()
+            engine = make_engine()
+            repo = UniverseRepo(engine)
+            members = repo.get_universe_members(env, strategy, as_of)
+            db_ok = bool(members)
+            print(f"[UNIVERSE][EMERGENCY_CHECK] as_of={as_of} db={'Y' if db_ok else 'N'}")
+            should_build = "0" if db_ok else "1"
+            with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as f:
+                f.write(f"SHOULD_BUILD_UNIVERSE={should_build}\n")
+            PY
+          else
+            echo "SHOULD_BUILD_UNIVERSE=0" >> "$GITHUB_ENV"
+            echo "[UNIVERSE][EMERGENCY_SKIP] reason=not_requested"
+          fi
+
+      - name: Build universe (best_k_meta)
+        if: env.SHOULD_BUILD_UNIVERSE == '1'
+        working-directory: repo
+        run: |
+          set -euo pipefail
+          python -m trader.universe.build --env "${KIS_ENV}" --strategy best_k_meta --date "${AS_OF_DATE}"
+
+      - name: Verify universe save
+        if: env.SHOULD_BUILD_UNIVERSE == '1'
+        working-directory: repo
+        run: |
+          set -euo pipefail
+          python - << 'PY'
+          from datetime import date
+          import os
+
+          from trader.db.engine import make_engine
+          from trader.db.repos import UniverseRepo
+          from trader.universe.lkg_store import lkg_path
+
+          env = (os.getenv("KIS_ENV") or "practice").lower()
+          strategy = os.getenv("PB1_UNIVERSE_STRATEGY") or "best_k_meta"
+          as_of = os.getenv("AS_OF_DATE") or date.today().isoformat()
+          engine = make_engine()
+          repo = UniverseRepo(engine)
+          members = repo.get_universe_members(env, strategy, as_of)
+          db_ok = bool(members)
+          lkg_ok = lkg_path(env, strategy).exists()
+          print(
+              "[UNIVERSE][SAVE] lkg=%s db=%s as_of=%s members=%s"
+              % ("Y" if lkg_ok else "N", "Y" if db_ok else "N", as_of, len(members))
+          )
+          PY
+
+      - name: Configure git identity for botstate commits
+        working-directory: repo
+        run: |
+          git config --global user.email "actions@github.com"
+          git config --global user.name "github-actions[bot]"
+
+      - name: Persist bot_state (fetch_reset)
+        if: env.SHOULD_BUILD_UNIVERSE == '1'
+        working-directory: repo
+        run: |
+          set -euo pipefail
+          python -m trader.botstate_sync persist --mode FETCH_RESET --remote origin --branch bot-state
+        env:
+          BOTSTATE_WORKTREE_DIR: "${{ github.workspace }}/_botstate"
+
+  trade_tick:
+    runs-on: ubuntu-latest
+    if: github.event_name != 'schedule' || github.event.schedule != '45 23 * * 0-4'
     timeout-minutes: 8
 
     env:
       TZ: Asia/Seoul
       PYTHONUNBUFFERED: "1"
       PYTHONPATH: ${{ github.workspace }}/repo
       FORCE_STRATEGY_MODE: ""
       FORCE_PB1_PHASE: ""
       STATE_PATH: "bot_state/runtime/state.json"
       LOT_STATE_PATH: "bot_state/runtime/lot_state.json"
       PBCORE_DB_PATH: "bot_state/db/pbcore.sqlite3"
       TRADER_CACHE_ROOT: "bot_state/runtime"
       UNIVERSE_LKG_DIR: "bot_state/universe_lkg"
       ALLOW_UNIVERSE_BUILD_IN_TRADE: "0"
       COMPILE_CHECK_ENABLED: "0"
       MIGRATE_MODE: "AUTO"
       PERSIST_MODE: "ON_CHANGE"
       BOTSTATE_WORKTREE_DIR: "${{ github.workspace }}/_botstate"
 
       # ====== ✅ repo에 이미 존재하는 secrets 이름 그대로 사용 ======
       KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
       KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
       KIS_REST_URL: ${{ secrets.KIS_REST_URL }}
       KIS_ENV: ${{ secrets.KIS_ENV }}
       CANO: ${{ secrets.CANO }}
@@ -171,112 +337,63 @@ jobs:
               elif time(9, 5) <= now_t < time(10, 0):
                   window = "morning"
               elif time(10, 0) <= now_t < time(15, 15):
                   window = "day"
               elif time(15, 15) <= now_t <= time(15, 30):
                   window = "close"
               else:
                   window = "after"
 
           if force_phase in {"entry", "exit", "verify"}:
               pb1_phase = force_phase
           else:
               pb1_phase = "entry" if window in {"preopen", "morning", "day"} else "exit"
 
           print(
               f"[AUTO] now={now.isoformat()} trading_day={is_weekday} window={window} "
               f"in_market={in_market} -> STRATEGY_MODE={mode} PB1_PHASE_DEFAULT={pb1_phase}"
           )
           with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as f:
               f.write(f"EFFECTIVE_STRATEGY_MODE={mode}\n")
               f.write(f"TRADING_DAY={'true' if is_weekday else 'false'}\n")
               f.write(f"MARKET_WINDOW={window}\n")
               f.write(f"PB1_PHASE_DEFAULT={pb1_phase}\n")
           PY
 
-      - name: Universe policy (08:45 only) + LKG safety gate
-        working-directory: repo
-        run: |
-          set -euo pipefail
-          today="$(TZ=Asia/Seoul date +%F)"
-          hhmm="$(TZ=Asia/Seoul date +%H:%M)"
-          dow="$(TZ=Asia/Seoul date +%u)"
-          allow_build=0
-          if [ "$dow" -ge 1 ] && [ "$dow" -le 5 ] && [ "$hhmm" = "08:45" ]; then
-            allow_build=1
-          fi
-          lkg="$(find "${UNIVERSE_LKG_DIR}" -maxdepth 6 -type f -name "latest.json" 2>/dev/null | head -n 1 || true)"
-          lkg_exists=0
-          if [ -n "$lkg" ]; then
-            lkg_exists=1
-          fi
-          should_build=$allow_build
-          in_market=0
-          if [ "${EFFECTIVE_STRATEGY_MODE:-}" = "LIVE" ] && [[ "${MARKET_WINDOW:-}" =~ ^(preopen|morning|day|close)$ ]]; then
-            in_market=1
-          fi
-          block=0
-          if [ "$in_market" = "1" ] && [ "$should_build" = "0" ] && [ "$lkg_exists" = "0" ]; then
-            block=1
-          fi
-          echo "SHOULD_BUILD_UNIVERSE=$should_build" >> "$GITHUB_ENV"
-          echo "BLOCK_TRADING_NO_LKG=$block" >> "$GITHUB_ENV"
-          if [ "${GITHUB_EVENT_NAME:-}" = "push" ] || [ "$should_build" = "1" ]; then
-            echo "COMPILE_CHECK_ENABLED=1" >> "$GITHUB_ENV"
-          else
-            echo "COMPILE_CHECK_ENABLED=0" >> "$GITHUB_ENV"
-          fi
-          echo "[UNIVERSE][POLICY] kst_hhmm=$hhmm dow=$dow allow_build=$allow_build in_market=$in_market lkg_exists=$lkg_exists -> build=$should_build block_trading=$block event=${GITHUB_EVENT_NAME:-}"
-
-      - name: Build universe (best_k_meta)
-        if: env.SHOULD_BUILD_UNIVERSE == '1'
-        working-directory: repo
-        run: |
-          set -euo pipefail
-          python -m trader.universe.build --env "${KIS_ENV}" --strategy "best_k_meta" --date "$(date +%F)"
-          echo "$(TZ=Asia/Seoul date +%F)" > "${UNIVERSE_LKG_DIR}/last_build_date.txt"
-
       - name: Compile check (gated)
         if: env.COMPILE_CHECK_ENABLED == '1'
         working-directory: repo
         run: |
           set -euo pipefail
           python -m compileall trader
 
       - name: Configure git identity for botstate commits
         working-directory: repo
         run: |
           git config --global user.email "actions@github.com"
           git config --global user.name "github-actions[bot]"
 
-      - name: Fail fast if no universe LKG during market hours
-        if: env.BLOCK_TRADING_NO_LKG == '1'
-        working-directory: repo
-        run: |
-          echo "::error::Market is open but universe LKG is missing. Universe build is forbidden now. Wait for next 08:45 KST build."
-          exit 1
-
       - name: Run PB1 (auto window) - should exit within 4 minutes
         working-directory: repo
         run: |
           set -euo pipefail
           echo "[RUN] STRATEGY_MODE=$EFFECTIVE_STRATEGY_MODE MARKET_WINDOW=$MARKET_WINDOW PB1_PHASE_DEFAULT=$PB1_PHASE_DEFAULT"
           python -m trader.run --window auto --phase auto
         env:
           STRATEGY_MODE: ${{ env.EFFECTIVE_STRATEGY_MODE }}
           LIVE_TRADING_ENABLED: "1"
 
       - name: Debug bot_state contents (after run)
         run: |
           set -euo pipefail
           echo "=== bot_state tree ==="
           find bot_state -maxdepth 3 -type f -print || true
           echo "=== git status (bot-state worktree if exists) ==="
           git status --porcelain || true
 
       - name: Debug paths (where am I?)
         run: |
           set -euo pipefail
           echo "PWD=$(pwd)"
           ls -al
           echo "---- find bot_state anywhere ----"
           find . -maxdepth 4 -type d -name "bot_state" -o -name "_botstate" -o -name "repo" -o -name "botstate" || true
diff --git a/trader/pb1_runner.py b/trader/pb1_runner.py
index 2ba260ab80b5760defc77e63cc2303f0742f0fb9..79b24d15086895d515db816a0d6233c2ad9620fa 100644
--- a/trader/pb1_runner.py
+++ b/trader/pb1_runner.py
@@ -21,51 +21,51 @@ from trader.config import (
     MORNING_EXIT_END,
     MORNING_EXIT_START,
     MORNING_WINDOW_END,
     MORNING_WINDOW_START,
     PB1_FORCE_ENTRY_ON_PUSH,
     PB1_MAX_WAIT_FOR_WINDOW_MIN,
     PB1_WAIT_FOR_WINDOW,
     resolve_strategy_mode,
 )
 from trader.botstate_paths import get_botstate_root
 from trader.botstate_sync import (
     acquire_lock as acquire_botstate_lock,
     compute_lock_ttl,
     persist_run_files,
     release_lock as release_botstate_lock,
     resolve_botstate_worktree_dir,
     setup_worktree,
 )
 from trader.db.engine import make_engine
 from trader.db.lock import release_lock, try_acquire_lock
 from trader.db.migrate import run_migrations
 from trader.db.repos import FillsRepo, LedgerEventsRepo, OrdersRepo, PositionsRepo, RunsRepo, UniverseRepo
 from trader.kis_wrapper import KisAPI
 from trader.pb1_engine import PB1Engine, resolve_pb1_phase
 from trader.reconcile_kis import reconcile_today
-from trader.runtime_store import universe_today_exists
+from trader.runtime_store import check_universe_ready
 from trader.time_utils import now_kst
 from trader.utils.env import env_bool, parse_env_flag, resolve_mode
 from trader.window_router import WindowDecision, decide_window
 
 logger = logging.getLogger(__name__)
 
 
 def _resolve_market_context(
     *,
     now: datetime,
     trading_day: bool,
     market_window: str,
     window_override: str,
     phase_seed: str | None,
 ) -> tuple[WindowDecision | None, str, str, str, str, list[str]]:
     window = decide_window(now=now, override=window_override)
     window_label = _resolve_window_label(market_window, window)
     resolved_phase, phase_reason, phase_window = resolve_pb1_phase(now, trading_day, phase_seed)
     reasons: list[str] = []
     if not resolved_phase:
         fallback = (os.getenv("PB1_PHASE_DEFAULT") or "entry").strip().lower()
         resolved_phase = fallback if fallback else "entry"
         phase_reason = "default_env"
         reasons.append("phase_default_env")
     reasons.append(f"market_window:{market_window}")
@@ -593,53 +593,56 @@ def run_once(
         if diag_enabled:
             logger.warning("[PB1][DIAG] non-trading-day(%s) but running diagnostics", now.date())
 
     owner = os.getenv("GITHUB_ACTOR", "local")
     workflow_run_id = os.getenv("GITHUB_RUN_ID", "local")
     lock_key = f"PB1:{kis_env_raw or 'practice'}"
     lock_acquired = try_acquire_lock(engine, lock_key)
     if not lock_acquired:
         logger.warning("[PB1][LOCKED] key=%s owner=%s run_id=%s", lock_key, owner, workflow_run_id)
         return [], False, {}, phase_for_log, "LOCKED"
 
     runs_repo = RunsRepo(engine)
     universe_repo = UniverseRepo(engine)
     orders_repo = OrdersRepo(engine)
     fills_repo = FillsRepo(engine)
     positions_repo = PositionsRepo(engine)
     ledger_repo = LedgerEventsRepo(engine)
 
     run_record_id = None
     did_work = False
     touched_files: list[Path] = []
     result = None
     run_start_ts = time_mod.time()
     try:
         if not close_cancel_only and trading_day and market_window in {"preopen", "morning", "day", "close"}:
-            if not universe_today_exists(now.date()):
-                logger.info("[PB1][SKIP] reason=universe_not_ready date=%s", now.date().isoformat())
-                return [], False, {}, phase_for_log, "SKIPPED"
+            universe_status = check_universe_ready(now.date())
+            if not universe_status["ok"]:
+                if phase_for_log == "entry":
+                    os.environ["PB1_ENTRY_ENABLED"] = "0"
+                    logger.info("[PB1][ENTRY_BLOCKED] reason=universe_not_ready date=%s", now.date().isoformat())
+                logger.info("[PB1][EXIT_FORCE_RUN] reason=universe_not_ready date=%s", now.date().isoformat())
         kis: KisAPI | None = None
         try:
             kis = KisAPI()
             if kis.env != kis_env:
                 dry_run_reasons.append("kis_env_mismatch")
                 dry_run = True
                 _apply_env_flags(dry_run)
         except Exception:
             logger.exception("[PB1] KIS init failed, forcing dry-run")
             dry_run_reasons.append("kis_init_failed")
             dry_run = True
             _apply_env_flags(dry_run)
 
         run_record_id = runs_repo.start_run(
             env=kis_env or "practice",
             strategy="pb1_pullback_close",
             run_window=window_label,
             phase=phase_override_arg,
             event_name=event_name_lower,
             dry_run=dry_run,
             git_sha=os.getenv("GITHUB_SHA"),
             workflow=os.getenv("GITHUB_WORKFLOW"),
             workflow_run_id=workflow_run_id,
             workflow_attempt=int(os.getenv("GITHUB_RUN_ATTEMPT", "0") or 0),
             config_json={
diff --git a/trader/runtime_store.py b/trader/runtime_store.py
index c2b03addaf2b6c69b51a3990f240294b5c676658..cd8f7d3db048bce1d13eec6e9336be9e5db49242 100644
--- a/trader/runtime_store.py
+++ b/trader/runtime_store.py
@@ -1,33 +1,37 @@
 from __future__ import annotations
 
 import logging
 import os
 from datetime import date
 
 from trader.db.engine import make_engine
 from trader.db.repos import UniverseRepo
 from trader.universe.lkg_store import lkg_path
 
 logger = logging.getLogger(__name__)
 
 DEFAULT_UNIVERSE_STRATEGY = "best_k_meta"
 
 
-def universe_today_exists(today: date) -> bool:
+def check_universe_ready(today: date) -> dict[str, bool]:
     env = (os.getenv("KIS_ENV") or "practice").lower()
     strategy = os.getenv("PB1_UNIVERSE_STRATEGY") or DEFAULT_UNIVERSE_STRATEGY
     as_of = today.isoformat()
     engine = make_engine()
     repo = UniverseRepo(engine)
     members = repo.get_universe_members(env, strategy, as_of)
     db_ok = bool(members)
     lkg_ok = lkg_path(env, strategy).exists()
     ok = db_ok and lkg_ok
     logger.info(
         "[UNIVERSE][CHECK] date=%s db=%s lkg=%s -> ok=%s",
         as_of,
         "Y" if db_ok else "N",
         "Y" if lkg_ok else "N",
         "Y" if ok else "N",
     )
-    return ok
+    return {"db_ok": db_ok, "lkg_ok": lkg_ok, "ok": ok}
+
+
+def universe_today_exists(today: date) -> bool:
+    return check_universe_ready(today)["ok"]


