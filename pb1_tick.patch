diff --git a/.github/workflows/trade-runner.yml b/.github/workflows/trade-runner.yml
index aedff4c32f11e566c07f92aa4c3c78ed7ac4feb8..998ecfe0c12ee193689b293ffb540400cc446bd3 100644
--- a/.github/workflows/trade-runner.yml
+++ b/.github/workflows/trade-runner.yml
@@ -9,96 +9,118 @@ concurrency:
 
 on:
   # ✅ push로도 즉시 돌고
   push:
     branches: [ "nullim", "main" ]
 
   # ✅ 수동 실행도 되고
   workflow_dispatch:
 
   # ✅ 5분 tick (스케줄 안정성을 위해 main에도 이 파일 존재 권장)
   schedule:
     - cron: "50-59/5 23 * * 0-4"
     - cron: "*/5 0-5 * * 1-5"
     - cron: "0-30/5 6 * * 1-5"
 
 jobs:
   run:
     runs-on: ubuntu-latest
     timeout-minutes: 8
 
     env:
       TZ: Asia/Seoul
       PYTHONUNBUFFERED: "1"
       FORCE_STRATEGY_MODE: ""
       FORCE_PB1_PHASE: ""
+      STATE_PATH: "bot_state/runtime/state.json"
+      PBCORE_DB_PATH: "bot_state/db/pbcore.sqlite3"
+      UNIVERSE_LKG_DIR: "bot_state/universe_lkg"
 
       # ====== ✅ repo에 이미 존재하는 secrets 이름 그대로 사용 ======
       KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
       KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
       KIS_REST_URL: ${{ secrets.KIS_REST_URL }}
       KIS_ENV: ${{ secrets.KIS_ENV }}
       CANO: ${{ secrets.CANO }}
       ACNT_PRDT_CD: ${{ secrets.ACNT_PRDT_CD }}
       KIS_ACCESS_TOKEN: ${{ secrets.KIS_ACCESS_TOKEN }}
       KIS_ACCOUNT: ${{ secrets.KIS_ACCOUNT }}
 
       # ====== ✅ 코드가 기대하는 이름으로 "매핑" (바꾸지 말고 추가만) ======
       APP_KEY: ${{ secrets.KIS_APP_KEY }}
       APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
       API_BASE_URL: ${{ secrets.KIS_REST_URL }}
 
       # ====== ✅ 5분 tick 설계: 4분만 실행하고 빠지기 ======
       RUN_LOOP_MINUTES: "4"
 
       # ====== ✅ BOTSTATE LOCK: 5분 틱에 맞게 ======
       BOTSTATE_LOCK_TTL_SEC: "240"
       BOTSTATE_LOCK_RETRY_TOTAL_SEC: "55"
       BOTSTATE_LOCK_RETRY_SLEEP_SEC: "5"
 
       ACTIVE_STRATEGIES: "1"
       DAILY_CAPITAL: "250000000"
 
     steps:
       - name: Checkout code (always run nullim code)
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
           ref: "nullim"
+          path: repo
+
+      - name: Checkout bot-state branch
+        uses: actions/checkout@v4
+        with:
+          fetch-depth: 0
+          ref: "bot-state"
+          path: botstate
+
+      - name: Restore bot_state into workspace
+        run: |
+          set -euo pipefail
+          mkdir -p repo/bot_state
+          if [ -d botstate/bot_state ]; then
+            rsync -a --delete botstate/bot_state/ repo/bot_state/
+          fi
+          echo "[RESTORE] repo/bot_state restored"
 
       - name: Setup Python
         uses: actions/setup-python@v5
         with:
           python-version: "3.11"
 
       - name: Install deps
+        working-directory: repo
         run: |
           set -euo pipefail
           python -m pip install --upgrade pip
           if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
 
       - name: Precheck env (fail fast)
+        working-directory: repo
         run: |
           set -euo pipefail
           echo "[PRECHECK] KIS_ENV=${KIS_ENV:-}"
           for k in APP_KEY APP_SECRET API_BASE_URL CANO ACNT_PRDT_CD KIS_ENV; do
             if [ -z "${!k:-}" ]; then
               echo "::error::Missing required secret/env: $k"
               exit 1
             fi
           done
           python -c "import importlib; importlib.import_module('trader.pb1_runner'); print('[PRECHECK] pb1_runner import ok')"
 
       - name: Resolve market-aware runtime mode
         run: |
           set -euo pipefail
           python - << 'PY'
           from datetime import datetime, time
           from zoneinfo import ZoneInfo
           import os
 
           KST = ZoneInfo("Asia/Seoul")
           now = datetime.now(KST)
 
           force_mode = (os.getenv("FORCE_STRATEGY_MODE") or "").strip().upper()
           force_phase = (os.getenv("FORCE_PB1_PHASE") or "").strip().lower()
 
@@ -118,49 +140,72 @@ jobs:
                   window = "morning"
               elif time(10, 0) <= now_t < time(15, 15):
                   window = "day"
               elif time(15, 15) <= now_t <= time(15, 30):
                   window = "close"
               else:
                   window = "after"
 
           if force_phase in {"entry", "exit", "verify"}:
               pb1_phase = force_phase
           else:
               pb1_phase = "entry" if window in {"preopen", "morning", "day"} else "exit"
 
           print(
               f"[AUTO] now={now.isoformat()} trading_day={is_weekday} window={window} "
               f"in_market={in_market} -> STRATEGY_MODE={mode} PB1_PHASE_DEFAULT={pb1_phase}"
           )
           with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as f:
               f.write(f"EFFECTIVE_STRATEGY_MODE={mode}\n")
               f.write(f"TRADING_DAY={'true' if is_weekday else 'false'}\n")
               f.write(f"MARKET_WINDOW={window}\n")
               f.write(f"PB1_PHASE_DEFAULT={pb1_phase}\n")
           PY
 
       - name: Build universe (best_k_meta) - allow fallback
+        working-directory: repo
         run: |
           set -euo pipefail
           python -m trader.universe.build --env "${KIS_ENV}" --strategy "best_k_meta" --date "$(date +%F)" || echo "[WARN] universe build failed (continue)"
 
       - name: Compile check
+        working-directory: repo
         run: |
           set -euo pipefail
           python -m compileall trader
 
       - name: Configure git identity for botstate commits
+        working-directory: repo
         run: |
           git config --global user.email "actions@github.com"
           git config --global user.name "github-actions[bot]"
 
       - name: Run PB1 (auto window) - should exit within 4 minutes
+        working-directory: repo
         run: |
           set -euo pipefail
           echo "[RUN] STRATEGY_MODE=$EFFECTIVE_STRATEGY_MODE MARKET_WINDOW=$MARKET_WINDOW PB1_PHASE_DEFAULT=$PB1_PHASE_DEFAULT"
           python -m trader.run --window auto --phase auto
         env:
           STRATEGY_MODE: ${{ env.EFFECTIVE_STRATEGY_MODE }}
           LIVE_TRADING_ENABLED: "1"
 
+      - name: Persist bot_state back to bot-state branch
+        if: always()
+        run: |
+          set -euo pipefail
+          rsync -a --delete repo/bot_state/ botstate/bot_state/
+
+          cd botstate
+          git config user.email "actions@github.com"
+          git config user.name "github-actions[bot]"
+          git add -A bot_state
+
+          if git diff --cached --quiet; then
+            echo "[PERSIST] no changes"
+            exit 0
+          fi
+
+          git commit -m "botstate: persist ${GITHUB_RUN_ID} $(date -Iseconds)"
+          git push origin HEAD:bot-state
+
           
diff --git a/trader/config.py b/trader/config.py
index e66148aec9f6bfc9d27e28868f724842309338b9..c6c532a197f854ea93d825e8b3bc778e127351b3 100644
--- a/trader/config.py
+++ b/trader/config.py
@@ -176,50 +176,51 @@ def _cfg(key: str) -> str:
 
 def _default_bool(key: str, fallback: bool = False) -> bool:
     raw_default = str(CONFIG.get(key, "")).strip().lower()
     if raw_default in TRUE_VALUES:
         return True
     if raw_default in FALSE_VALUES:
         return False
     return fallback
 
 
 def _cfg_bool(key: str, fallback: bool | None = None) -> bool:
     default_value = _default_bool(key, fallback if fallback is not None else False)
     return env_bool(key, default=default_value)
 
 
 logging.basicConfig(level=logging.INFO)
 logger = logging.getLogger(__name__)
 
 LOG_DIR = Path(__file__).parent / "logs"
 LOG_DIR.mkdir(exist_ok=True)
 STATE_FILE = Path(__file__).parent / "trade_state.json"  # legacy; position state uses STATE_PATH
 STATE_DIR_RAW = _cfg("STATE_DIR")
 STATE_DIR = Path(STATE_DIR_RAW) if STATE_DIR_RAW else Path(__file__).parent / "state"
 STATE_PATH = Path(_cfg("STATE_PATH") or STATE_DIR / "state.json")
 STATE_DIR.mkdir(parents=True, exist_ok=True)
+PBCORE_DB_PATH = os.getenv("PBCORE_DB_PATH", "bot_state/db/pbcore.sqlite3")
 STATE_PATH.parent.mkdir(parents=True, exist_ok=True)
 
 # 종목별 시장코드 고정 맵 (실전에서는 마스터테이블 로드로 대체 권장)
 MARKET_MAP: Dict[str, str] = {
     # 예시: '145020': 'J', '347850': 'J', '257720': 'U', '178320': 'J', '348370': 'U'
 }
 
 # 데이터 없음 1차 감지 상태 저장(연속 DATA_EMPTY 확인용)
 EXCLUDE_STATE: Dict[str, Dict[str, bool]] = {}
 
 KST = ZoneInfo("Asia/Seoul")
 
 SELL_FORCE_TIME_STR = _cfg("SELL_FORCE_TIME").strip()
 SELL_ALL_BALANCES_AT_CUTOFF = _cfg_bool("SELL_ALL_BALANCES_AT_CUTOFF")
 EMERGENCY_GLOBAL_SELL = _cfg_bool("EMERGENCY_GLOBAL_SELL")
 RATE_SLEEP_SEC = float(_cfg("API_RATE_SLEEP_SEC"))
 FORCE_SELL_PASSES_CUTOFF = int(_cfg("FORCE_SELL_PASSES_CUTOFF"))
 FORCE_SELL_PASSES_CLOSE = int(_cfg("FORCE_SELL_PASSES_CLOSE"))
 FORCE_SELL_BLOCKED_LOTS = _cfg_bool("FORCE_SELL_BLOCKED_LOTS")
 PARTIAL1 = float(_cfg("PARTIAL1"))
 PARTIAL2 = float(_cfg("PARTIAL2"))
 TRAIL_PCT = float(_cfg("TRAIL_PCT"))
 FAST_STOP = float(_cfg("FAST_STOP"))
 ATR_STOP = float(_cfg("ATR_STOP"))
 TIME_STOP_HHMM = _cfg("TIME_STOP_HHMM")
diff --git a/trader/db/config.py b/trader/db/config.py
index f105fd79e5997ab2ae03df86b23322b324ac1881..737484a9183e96a084111cd2a879f36bccd9edc7 100644
--- a/trader/db/config.py
+++ b/trader/db/config.py
@@ -1,32 +1,39 @@
 import os
 from pathlib import Path
 
 from sqlalchemy.engine.url import make_url
 
+from trader.config import PBCORE_DB_PATH
 
-DEFAULT_SQLITE_PATH = Path(os.getenv("UNIVERSE_SQLITE_PATH") or Path(__file__).resolve().parents[1] / "state" / "pbcore.sqlite3")
+
+DEFAULT_SQLITE_PATH = Path(
+    os.getenv("PBCORE_DB_PATH")
+    or os.getenv("UNIVERSE_SQLITE_PATH")
+    or PBCORE_DB_PATH
+    or Path(__file__).resolve().parents[1] / "state" / "pbcore.sqlite3"
+)
 
 
 def using_external_db() -> bool:
     return os.getenv("USING_EXTERNAL_DB", "0").lower() in {"1", "true", "yes", "on"}
 
 
 def _ensure_sqlite_parent(url: str) -> None:
     try:
         if not is_sqlite_url(url):
             return
         db_path = make_url(url).database
         if not db_path:
             return
         Path(db_path).expanduser().resolve().parent.mkdir(parents=True, exist_ok=True)
     except Exception:
         # Best-effort only; filesystem permissions may vary.
         return
 
 
 def get_database_url() -> str:
     url = os.getenv("DATABASE_URL")
     if using_external_db():
         if not url:
             raise RuntimeError("DATABASE_URL must be set when USING_EXTERNAL_DB=1")
         return url
diff --git a/trader/universe/lkg_store.py b/trader/universe/lkg_store.py
index 00392b21d1bf2b798b9d989292a1f64327b753bc..1cbece82cbc9cdad581e30324ce3f900cb35500c 100644
--- a/trader/universe/lkg_store.py
+++ b/trader/universe/lkg_store.py
@@ -1,38 +1,39 @@
 from __future__ import annotations
 
 import json
 import logging
 import os
 from datetime import datetime
 from pathlib import Path
 from typing import Optional
 
 logger = logging.getLogger(__name__)
 
 _LKG_ROOT = Path(
-    os.getenv("UNIVERSE_LKG_ROOT")
+    os.getenv("UNIVERSE_LKG_DIR")
+    or os.getenv("UNIVERSE_LKG_ROOT")
     or Path(__file__).resolve().parent.parent / "state" / "universe_lkg"
 )
 
 
 def lkg_path(env: str, strategy: str) -> Path:
     env_norm = (env or "practice").lower()
     strategy_norm = strategy or "default"
     return _LKG_ROOT / env_norm / strategy_norm / "latest.json"
 
 
 def _history_path(env: str, strategy: str, as_of: str | None) -> Path:
     env_norm = (env or "practice").lower()
     strategy_norm = strategy or "default"
     ts = as_of or datetime.utcnow().date().isoformat()
     return _LKG_ROOT / env_norm / strategy_norm / "history" / f"{ts}.json"
 
 
 def load_lkg(env: str, strategy: str) -> Optional[dict]:
     path = lkg_path(env, strategy)
     try:
         if not path.exists():
             logger.info("[UNIVERSE][LKG][MISS] env=%s strategy=%s path=%s", env, strategy, path)
             return None
         payload = json.loads(path.read_text(encoding="utf-8"))
         as_of = payload.get("params", {}).get("as_of") or payload.get("as_of")
